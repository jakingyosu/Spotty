# set of Discord ID's of users who can use dev commands on the bot
OWNERS = {519472950928211978}

# Default prefix the bot uses when it joins a server
DEFAULT_PREFIX = "!"

#token of the discord bot account
TOKEN = "token"

# the file where logs are kept
LOG_FILE = "./log.txt" 
